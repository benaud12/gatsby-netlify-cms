import React from "react"
import { Link } from "gatsby"

export default () => (
  <header className="navbar-wrapper">
    <nav className="navbar">
      <Link to="/">
        HOME
      </Link>
    </nav>
  </header>
)
